on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v4
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.6
      with:
        server: ssh-repoclase.alwaysdata.net
        username: repoclase
        password: Misifu123+
        server-dir: www/
        local-dir: ./
        exclude: |
          **/.git*
          **/.github*
          **/README.md
          **/node_modules/**
          **/.env
